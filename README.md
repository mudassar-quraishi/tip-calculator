# Tip Calculator

Simple Tip Calculator (React + Vite) — single-screen bill splitter.

Run locally:

```bash
npm install
npm run dev
```

Open the URL shown by Vite (e.g. http://localhost:5173/ or the port Vite picks).

Build for production:

```bash
npm run build
npm run preview
```
